# como no esquema podemos ter clustered \ shard tem que alterar a port para a que tem o S o esquema do writem concern
# spring.data.mongodb.uri = mongodb://localhost:27040?retryWrites=false  ou a padrão/blog ? precisar desativar as tentativas de reescrita, por isso e bom
spring.data.mongodb.uri = mongodb://localhost/blog?retryWrites=false
spring.data.mongodb.port =
spring.data.mongodb.username=
spring.data.mongodb.password=